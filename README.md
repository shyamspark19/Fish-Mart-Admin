# FishMart

FishMart — Fresh Catch. Fast Delivery.

This repository contains a full-stack fish delivery application scaffold with a `client/` (React + Vite + Tailwind) and `server/` (Node + Express + TypeScript + Mongoose).

See `client/` and `server/` folders for setup instructions.
